using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arya : Player
{
    // Start is called before the first frame update
    void Start()
    {
        health = 80;
        intelligence = 120;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
